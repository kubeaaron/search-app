apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-opensearch-dashboards
spec:
  selector:
    app: {{ .Release.Name }}-opensearch-dashboards
  ports:
    - port: 5601
      targetPort: 5601
